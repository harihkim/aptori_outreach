"""Workspace aggregate: the ownership boundary every other aggregate references."""

from app.workspaces.models import DEFAULT_WORKSPACE_ID, Workspace

__all__ = ["DEFAULT_WORKSPACE_ID", "Workspace"]
