# Browser-visible backend origin used by the SvelteKit server loads.
PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Private SvelteKit server token; use the same secret as backend APTORI_API_TOKEN.
API_TOKEN=replace-with-the-backend-token
